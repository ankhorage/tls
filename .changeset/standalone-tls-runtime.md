---
'@ankhorage/tls': minor
---

Make TLS storage host-owned and environment-agnostic, add native Certbot with Docker fallback, route CLI commands through composition boundaries, stream long-running Certbot output, add optional consumer guidance profiles, and run an explicit deployment hook only after certificate files actually renew.
