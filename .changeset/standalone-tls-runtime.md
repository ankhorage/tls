---
'@ankhorage/tls': minor
---

Make TLS storage host-owned and environment-agnostic, add native Certbot with Docker fallback, route CLI commands through composition boundaries, stream long-running Certbot output, and add optional consumer guidance profiles.
